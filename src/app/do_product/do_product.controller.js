const router = require('express').Router()

/**
 * 내적
 * @param {Array} a 숫자 배열
 * @param {Array} b 숫자 배열
 * @return {Number} 내적
 */

 function solution(a, b) {
    var answer = 1234567890;
    return answer;
}
module.exports = router;